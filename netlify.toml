[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/p/*"
  to = "/index.html"
  status = 200

# SPA fallback for custom public links like /my-page
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
